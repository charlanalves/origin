version: "2"
services:
  mochai-chai:
    container_name: "origin_test"
    build: .
    volumes:
      - ./:/TTD
    ports:
      - "8085:8084"   
    environment:
        WEB_PORT: "8085" # this is optional, allows express to use process.env.PORT instead   
   
